import SliderTemplate from "../template/SliderTemplate";

export default function PrivateBuilding() {
    return (
        <SliderTemplate 
        title="PRIVATE BUILDING"
        pathImages="/private-building"
        imageCount={9}
        autoplay={true}
        />
    )
}