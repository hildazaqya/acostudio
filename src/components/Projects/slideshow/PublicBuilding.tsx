import SliderTemplate from "../template/SliderTemplate";

export default function PublicBuilding() {
    return (
        <SliderTemplate 
        title="PUBLIC BUILDING"
        pathImages="/public-building"
        imageCount={5}
        autoplay={false}
        />
    )
}