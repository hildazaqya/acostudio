import SliderTemplate from "../template/SliderTemplate";

export default function SemiBuilding() {
    return (
        <SliderTemplate 
        title="SEMI BUILDING"
        pathImages="/semi-building"
        imageCount={7}
        autoplay={false}
        />
    )
}